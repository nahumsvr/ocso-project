info:
  name: get by name
  type: http
  seq: 6

http:
  method: GET
  url: "{{URL}}/locations/name/:name"
  params:
    - name: name
      value: Pepsico
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
