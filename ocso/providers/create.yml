info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL}}/providers"
  body:
    type: json
    data: |-
      {
        "providerName": "Coca Cola",
        "providerEmail": "cocacola@pepsico.com",
        "provider"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
