info:
  name: create
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/products"
  body:
    type: json
    data: |-
      {
        "productName": "Something else 3",
        "price": 23,
        "countSeal": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
