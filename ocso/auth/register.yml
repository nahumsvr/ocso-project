info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/auth/signup"
  body:
    type: json
    data: |
      {
        "userEmail":"loll@lol.com",
        "userPassword": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
