info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/auth/signin"
  body:
    type: json
    data: |
      {
        "userEmail":"loll@lol.com",
        "userPassword": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
